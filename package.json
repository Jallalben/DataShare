{
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "e2e": "docker compose -f docker-compose.yml -f docker-compose.e2e.yml --profile e2e up --abort-on-container-exit --exit-code-from cypress",
    "e2e:down": "docker compose -f docker-compose.yml -f docker-compose.e2e.yml --profile e2e down"
  },
  "devDependencies": {
    "cypress": "^15.13.1",
    "typescript": "^6.0.2"
  }
}
